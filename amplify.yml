# Hosting-only build: publishes the Vite app immediately.
# Backend deploy (pipeline-deploy) is temporarily skipped while SES/Cognito is fixed.
# Restore fullstack build in amplify.fullstack.yml when backend deploy succeeds.
version: 1
customRules:
  - source: '</^[^.]+$|\.(?!(css|gif|ico|jpg|js|png|txt|svg|woff|woff2|ttf|map|json|webp)$)([^.]+$)/>'
    target: '/index.html'
    status: '200'
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline --legacy-peer-deps
    build:
      commands:
        - cp deploy/ci-amplify-outputs.json amplify_outputs.json
        - npm run build
        - test -f dist/index.html || (echo "ERROR dist/index.html missing" && exit 1)
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
